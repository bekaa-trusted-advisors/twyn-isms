# C-2026-08-18-01 — Entregável #1: Migração SGPI 27701:2019 → 2025

| Campo | Valor |
|---|---|
| **Engajamento** | ENG-2026-001 |
| **Contrato** | C-2026-08-18-01 |
| **Objetivo** | Migrar o SGPI de 27701:2019 para 27701:2025 preservando o SGSI (27001:2022) e a evidência de privacidade reaproveitável |
| **Princípio** | *snapshot-first* + **rollback parcial** + cada escrita no nISO sob aprovação humana |
| **Fonte da verdade** | nISO (HTTP API — `https://niso.ness.workers.dev`) |

> Documento de trabalho do consultor. Não é auditoria (9.2) nem parecer de certificação.
> Nenhum status de conformidade é presumido.

---

## Plano (fases)

1. **Snapshot / baseline** *(read-only)* — capturar o retrato atual `:2019` (projeto, controles/SoA,
   readiness, ROPA, evidências) e registrá-lo em `engagement.md`. Confirmar que `db:backup` do nISO
   existe (rollback total) além do baseline documental (rollback parcial).
2. **Transição de estado** *(1 write, aprovado)* — status do projeto **`Audit Ready` → `In Progress`**.
   Justificativa: a migração reabre o SGPI; manter "Audit Ready" seria falso (ver reconciliação PR #2).
3. **Reabrir jornada SGPI 2025** *(writes, aprovados um a um)* — reabrir as fases de privacidade
   (`phase-answers`) sob a estrutura 2025; mapear cada fase 2019 → equivalente 2025.
4. **Delta de controles** *(writes, aprovados um a um)* — propor o remapeamento de controles de
   privacidade 2019 → 2025 via `control-adequacao` (+ `/apply`); ver `soa/SGPI-SoA-27701-2025.md`.
5. **Preservar 27001 + evidência reaproveitável** — **não** tocar nos controles 27001:2022; marcar a
   evidência de privacidade existente como candidata a reaproveitamento (verificar validade, não recriar).
6. **Re-baseline** — recalcular readiness sob 2025 e registrar novo snapshot.

## Rollback parcial

| Nível | Mecanismo |
|---|---|
| Documental | `engagement.md` guarda o baseline `:2019`; reverter campo a campo |
| Repo | âncora `main @ 6697267` (estado pré-reconciliação) |
| nISO | `db:backup` (rollback total do backend, informado pelo cliente) |
| Por escrita | cada mutação é atômica e aprovada isoladamente → reversível individualmente |

## Mapa de endpoints (a confirmar o verbo/shape via GET primeiro)

**Read (verificação):**
- `GET /api/v1/projects/mr9c1qugo16zic2eko`
- `GET /api/v1/controls?project_id=mr9c1qugo16zic2eko`
- `GET /api/v1/projects/mr9c1qugo16zic2eko/readiness-check`
- `GET /api/v1/projects/mr9c1qugo16zic2eko/ropa`
- `GET /api/v1/projects/mr9c1qugo16zic2eko/evidence`

**Write (cada uma: proposta → aprovação → apply):**
- controles/SoA: `/api/v1/controls`, `/api/v1/projects/:id/control-adequacao` (+ `/apply`)
- riscos: `/api/v1/...` (risks)
- fases/jornada: `/api/v1/projects/:id/phase-answers`
- certificação/status: `/api/v1/projects/:id/certification`

> `403` = a policy de papel (consultant) barrou aquele path — registrar e não insistir.

## Runbook de verificação (rodar quando `NISO_API_KEY` estiver injetada)

```bash
# A key vem do ambiente da sessão; NUNCA ecoar o valor.
BASE="https://niso.ness.workers.dev"; PID="mr9c1qugo16zic2eko"
H=(-H "X-API-Key: $NISO_API_KEY" -H "Accept: application/json")
for p in \
  "/api/v1/projects/$PID" \
  "/api/v1/controls?project_id=$PID" \
  "/api/v1/projects/$PID/readiness-check" \
  "/api/v1/projects/$PID/ropa" \
  "/api/v1/projects/$PID/evidence" ; do
  echo "== GET $p =="
  curl -sS -o /dev/null -w "HTTP %{http_code}\n" "${H[@]}" "$BASE$p"
done
```

## Estado atual

- ⛔ **Bloqueado**: `NISO_API_KEY` UNSET nesta sessão. Nenhum GET/POST executado.
- Próximo passo ao receber a key: rodar o runbook (read), preencher o baseline em `engagement.md`,
  e **só então** propor a Fase 2 (transição de status) para aprovação.
